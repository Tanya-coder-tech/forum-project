# Deploy Project on Localhost - TODO

- [x] Create `docker-compose.yml` in project root
- [x] Update `backend/package.json` to add `"start": "node index.js"` script
- [x] Update `frontend/vite.config.js` to expose host and proxy API requests to backend
- [x] Run `docker-compose up --build` and verify services

